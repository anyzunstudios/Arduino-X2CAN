# Arduino-X2CAN
Arduino wrapper for the MCP2515. It blends the simple Seeed-style API with the robust autowp driver, supporting up to four CAN controllers, interrupts or polling, raw frames, masks, filters, and reliable 3.3V setups using TXS0108E.
